#初始化
:Init

{delayTime = 0.1}

{
	print("===SceneTable===")
	Sprite.create("background", "image")
	print("===_ENV===")
	for key, value in pairs(_ENV) do
		print(key)
	end

	print("===_G===")
	for key, value in pairs(_G) do
		print(key)
	end
	
	print("===Domain===")
	print(domain)
	print("================")
}

#:CreateSprite name="background" type=image
==Begin==

:SetSprite name="background" image="Logo.png"
Domain{return domain}

:Wait time={return delayTime}
:SetSprite name="background" image=""
:Wait time={return delayTime}

{delayTime = delayTime + 0.1}

:Goto pos="Begin"

==Exit==
